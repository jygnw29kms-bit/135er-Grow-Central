#!/bin/sh
set -e
if command -v systemctl >/dev/null 2>&1; then systemctl daemon-reload || true; fi
if [ "$1" = purge ]; then
 rm -rf /var/lib/etes-werkstattplaner
 deluser --system werkstattplaner >/dev/null 2>&1 || true
 delgroup --system werkstattplaner >/dev/null 2>&1 || true
fi
exit 0
