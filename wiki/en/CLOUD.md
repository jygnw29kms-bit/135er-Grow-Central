# Cloud

The VPS layer is optional. It can provide telemetry ingestion, long-term history, users/RBAC, multi-site overview, command queue, audit and backups.

The Pi connects outbound over HTTPS. No local control port is exposed to the Internet. Remote commands require both server-side and local opt-in and are validated on the Pi.
