# Sensors and History

Each sensor has an ID, site, type, unit, source, status and optional calibration data. Typical values include temperature, relative humidity, VPD, dew point, fan/device metrics and Raspberry Pi system metrics.

History stores timestamped readings. Local retention can be shorter while the cloud stores longer-term data. Validate ranges, timestamps and offline state.
